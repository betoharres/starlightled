<%= form_for @network, :html => { :class => 'form-horizontal form-model' } do |f| %>
  <div class="form-group">
    <!--<%= f.label :name, :class => 'control-label col-md-2' %>-->
	<label class="control-label col-md-2" for="network_name">Nome</label>
    <div class="col-md-10">
      <%= f.text_field :name, :class => 'text-field form-control' %>
    </div>
  </div>

  <%= render 'tags/tag', f: f, model: @network %>

  <div class="form-group">
    <div class='col-md-offset-2 col-md-10'>
      <!--<%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                  networks_path, :class => 'btn btn-default' %>
	  -->
	  
	  <%= f.submit nil, :value => 'Salvar', :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => "Cancelar"),
                  networks_path, :class => 'btn btn-default' %>
    </div>
  </div>
<% end %>
